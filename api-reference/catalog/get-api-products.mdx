---
title: "Get products"
description: "Fetch products with optional filters. `GET /api/products` and the legacy `GET /api/products/product` path are the same handler."
openapi: "GET /api/products"
---
